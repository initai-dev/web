# Makefile Guidelines - Bliss Framework Style

## Overview
• Use Makefiles for project automation and task management
• Follow consistent style with informative console output
• Structure with variables, organized sections, and helpful aliases
• Prioritize developer experience and clarity

## File Structure

### Header
• Start with project description comment
• Declare .PHONY targets early
• List all main phony targets in one line

### Variables Section
• Define all variables at the top after .PHONY
• Use descriptive UPPER_CASE names
• Group related variables together
• Include version, ports, image names, paths

### Target Organization
1. **help** - Always first target (default)
2. **Core targets** - Main functionality (dev, build, test)
3. **Docker targets** - Container operations
4. **Utility targets** - Clean, validate, info
5. **Workflow targets** - Setup, deployment checks
6. **Aliases** - Quick commands and shortcuts

## Naming Conventions

### Target Names
• Use kebab-case: `docker-build`, `dev-setup`
• Be descriptive and consistent
• Group related targets with prefixes

### Variables
• Use UPPER_SNAKE_CASE: `IMAGE_NAME`, `DOCKER_PORT`
• Be explicit and descriptive
• Include units in names if relevant: `TIMEOUT_SECONDS`

## Console Output Style

### Emojis and Formatting
• Use emojis for visual clarity and personality
• Common patterns:
  - 🚀 Starting/Running
  - 🔨 Building
  - 🧪 Testing
  - 🐳 Docker operations
  - ✅ Success/Complete
  - ❌ Error/Failed
  - 🧹 Cleaning
  - 📦 Installing/Dependencies
  - 🔍 Validating/Checking
  - ⚠️ Warning
  - 📋 Information
  - 🛑 Stopping

### Output Messages
• Start with action: "🚀 Starting development server..."
• End with result: "✅ Server started at http://localhost:3000"
• Use @ prefix to suppress command echo
• Provide helpful next steps when relevant

## Standard Patterns

### Help Target (Always Default)
```makefile
help: ## Show this help message
	@echo "Project Name - Description"
	@echo ""
	@echo "Available targets:"
	@awk 'BEGIN {FS = ":.*?## "} /^[a-zA-Z_-]+:.*?## / {printf "  %-15s %s\n", $$1, $$2}' $(MAKEFILE_LIST)
```

### Variables Section
```makefile
# Variables
PROJECT_NAME := my-project
VERSION := 1.0.0
DEV_PORT := 3000
DOCKER_PORT := 8080
IMAGE_NAME := $(PROJECT_NAME)
```

### Docker Targets
```makefile
docker-build: ## Build Docker image
	@echo "🐳 Building Docker image..."
	docker build -t $(IMAGE_NAME):$(VERSION) .
	@echo "✅ Docker image built: $(IMAGE_NAME):$(VERSION)"

docker-run: docker-build ## Run Docker container
	@echo "🚀 Running Docker container on port $(DOCKER_PORT)..."
	docker run -d -p $(DOCKER_PORT):80 --name $(PROJECT_NAME) $(IMAGE_NAME):latest
	@echo "✅ Container started at http://localhost:$(DOCKER_PORT)"

docker-stop: ## Stop and remove Docker container
	@echo "🛑 Stopping Docker container..."
	docker stop $(PROJECT_NAME) 2>/dev/null || true
	docker rm $(PROJECT_NAME) 2>/dev/null || true
	@echo "✅ Container stopped and removed"
```

### Development Targets
```makefile
dev: ## Start development server
	@echo "🚀 Starting development server..."
	npm run dev

build: ## Build for production
	@echo "🔨 Building for production..."
	npm run build
	@echo "✅ Build complete in ./dist/"

test: ## Run tests
	@echo "🧪 Running tests..."
	npm test
	@echo "✅ Tests completed"
```

### Utility Targets
```makefile
clean: ## Clean build artifacts
	@echo "🧹 Cleaning build artifacts..."
	rm -rf dist/ node_modules/.cache/
	@echo "✅ Cleaned"

setup: install-deps ## Setup development environment
	@echo "🛠️ Setting up development environment..."
	@echo "✅ Development environment ready"

info: ## Show project information
	@echo "📋 Project Information:"
	@echo "  Name: $(PROJECT_NAME)"
	@echo "  Version: $(VERSION)"
	@echo "  Dev port: $(DEV_PORT)"
```

## Best Practices

### Error Handling
• Use `|| true` for commands that might fail safely
• Suppress stderr with `2>/dev/null` when appropriate
• Check for required tools before using them

### Dependencies
• Use target dependencies: `docker-run: docker-build`
• Create logical dependency chains
• Consider using order-only prerequisites when needed

### Aliases and Convenience
• Provide common aliases: `run: dev`, `start: dev`
• Include `setup` as alias for dependency installation
• Add `stop` target for shutting down services

### Documentation
• Always include ## comments for help target
• Use descriptive target names that don't need explanation
• Add comments for complex operations

## Example Template
```makefile
# Project Name - Description
# Brief description of what this Makefile does

.PHONY: help dev build test clean setup

# Variables
PROJECT_NAME := my-project
VERSION := 1.0.0
DEV_PORT := 3000
DOCKER_PORT := 8080

# Default target
help: ## Show this help message
	@echo "$(PROJECT_NAME) - Description"
	@echo ""
	@echo "Available targets:"
	@awk 'BEGIN {FS = ":.*?## "} /^[a-zA-Z_-]+:.*?## / {printf "  %-15s %s\n", $$1, $$2}' $(MAKEFILE_LIST)

# Core targets
dev: ## Start development server
	@echo "🚀 Starting development server..."
	# Your dev command here

build: ## Build for production
	@echo "🔨 Building for production..."
	# Your build command here

test: ## Run tests
	@echo "🧪 Running tests..."
	# Your test command here

# Utility targets
clean: ## Clean build artifacts
	@echo "🧹 Cleaning..."
	# Your clean commands here

setup: install-deps ## Setup development environment
	@echo "🛠️ Setting up..."
	# Your setup commands here

# Aliases
run: dev ## Alias for dev
start: dev ## Alias for dev
```

## Framework-Specific Considerations

### Node.js Projects
• Use `npm` or `yarn` commands consistently
• Include `install-deps` target for `npm install`
• Consider `lint`, `format`, `type-check` targets

### Docker Projects
• Always include build, run, stop targets
• Use consistent naming for containers
• Include health checks where appropriate

### Web Projects
• Include development server targets
• Consider build optimization targets
• Include deployment-related targets

## Remember
• Makefiles should make development easier, not harder
• Use consistent emoji and formatting patterns
• Provide helpful output messages
• Test targets should be reliable and fast
• Always include a helpful `help` target as default